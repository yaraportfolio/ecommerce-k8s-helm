{{- if and .Values.services.orderService.enabled .Values.services.orderService.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.services.orderService.name }}-hpa
  namespace: {{ .Values.global.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.services.orderService.name }}
  minReplicas: {{ .Values.services.orderService.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.services.orderService.autoscaling.maxReplicas }}
  metrics:
  {{- if .Values.services.orderService.autoscaling.targetCPUUtilizationPercentage }}
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.services.orderService.autoscaling.targetCPUUtilizationPercentage }}
  {{- end }}
  {{- if .Values.services.orderService.autoscaling.targetMemoryUtilizationPercentage }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ .Values.services.orderService.autoscaling.targetMemoryUtilizationPercentage }}
  {{- end }}
{{- end }}
