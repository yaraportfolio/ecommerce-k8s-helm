# ============================================================
# HELM CHART v3.0 - Microservices E-Commerce
# Support Harbor + Docker Hub + Registries Custom
# ============================================================

# Configuration globale
global:
  namespace: ecommerce

# ============================================================
# REGISTRY CONFIGURATION (GitHub Container Registry)
# ============================================================
image:
  # Mode registry : "ghcr" (GitHub Container Registry)
  registryType: ghcr  # GitHub Container Registry (public)

  # Configuration GitHub Container Registry
  ghcr:
    registry: ghcr.io
    owner: yaraportfolio  # GitHub username/org

  # Configuration générale
  pullPolicy: Always

  # Image Pull Secrets
  # Si votre repo est public, laisser vide []
  imagePullSecrets:
    enabled: false  # false pour GHCR public

# ============================================================
# DATABASE CONFIGURATION
# ============================================================
database:
  host: "192.168.56.115"
  port: 3306
  name: ecommerce_db
  user: devops_user
  # ⚠️ NE PAS mettre le password ici en production !
  # Utiliser : --set database.password=XXX ou values-secrets.yaml
  password: ""  # Vide par défaut (sécurité)

# ============================================================
# JWT SECRET
# ============================================================
jwt:
  # ⚠️ NE PAS mettre le secret ici en production !
  # Utiliser : --set jwt.secret=XXX ou values-secrets.yaml
  secret: ""  # Vide par défaut (sécurité)

# ============================================================
# MICROSERVICES CONFIGURATION
# ============================================================
services:
  # Auth Service
  authService:
    enabled: true
    name: auth-service
    image:
      # Tag peut être : v3.0.0, latest, 1a2b3c4 (commit SHA)
      tag: latest
      # Override registry pour ce service (optionnel)
      # registry: custom.registry.com
    port: 3001
    replicas: 2
    resources:
      requests:
        memory: "128Mi"
        cpu: "100m"
      limits:
        memory: "256Mi"
        cpu: "200m"
    # Autoscaling (HPA)
    autoscaling:
      enabled: true
      minReplicas: 1
      maxReplicas: 3
      targetCPUUtilizationPercentage: 70
      targetMemoryUtilizationPercentage: 80
    
  # Product Service
  productService:
    enabled: true
    name: product-service
    image:
      tag: latest
    port: 3002
    replicas: 2
    resources:
      requests:
        memory: "128Mi"
        cpu: "100m"
      limits:
        memory: "256Mi"
        cpu: "200m"
    autoscaling:
      enabled: true
      minReplicas: 1
      maxReplicas: 3
      targetCPUUtilizationPercentage: 70
    
  # Order Service
  orderService:
    enabled: true
    name: order-service
    image:
      tag: latest
    port: 3003
    replicas: 1
    resources:
      requests:
        memory: "128Mi"
        cpu: "100m"
      limits:
        memory: "256Mi"
        cpu: "200m"
    autoscaling:
      enabled: true
      minReplicas: 1
      maxReplicas: 3
      targetCPUUtilizationPercentage: 70
    
  # Review Service
  reviewService:
    enabled: true
    name: review-service
    image:
      tag: latest
    port: 3004
    replicas: 1
    resources:
      requests:
        memory: "128Mi"
        cpu: "100m"
      limits:
        memory: "256Mi"
        cpu: "200m"
    autoscaling:
      enabled: true
      minReplicas: 1
      maxReplicas: 2
      targetCPUUtilizationPercentage: 70

# ============================================================
# INGRESS CONFIGURATION
# ============================================================
ingress:
  enabled: true
  className: nginx
  annotations: {}
  host: api.ecommerce.local
  tls:
    enabled: false
    secretName: microservices-tls

# ============================================================
# MONITORING (Prometheus)
# ============================================================
monitoring:
  # Activer le monitoring
  enabled: false
  
  # ServiceMonitor pour Prometheus Operator
  serviceMonitor:
    enabled: false
    interval: 30s
    scrapeTimeout: 10s
    labels:
      monitoring: "true"
      prometheus: "kube-prometheus"

# ============================================================
# AWS SECRETS MANAGER (EKS avec Secrets Store CSI Driver)
# ============================================================
awsSecretsManager:
  # Activer pour EKS sur AWS (utilise AWS Secrets Manager + ASCP add-on)
  # Désactiver (false) pour on-premise / dev (utilise Kubernetes Secrets)
  enabled: true

  # Région AWS (requise si enabled: true)
  region: "eu-west-1"

  # IAM Role ARN pour IRSA (IAM Roles for Service Accounts)
  # Format: arn:aws:iam::ACCOUNT_ID:role/ecommerce-eks-secrets-role
  # Laisser vide si disabled
  iamRoleArn: "arn:aws:iam::020379956170:role/ecommerce-eks-secrets-role"

  # Noms des secrets AWS Secrets Manager
  secrets:
    dbCredentials:
      # Secret contenant : { "DB_USER": "...", "DB_PASSWORD": "..." }
      secretName: "ecommerce/db/credentials"
    jwtSecret:
      # Secret contenant : { "JWT_SECRET": "..." }
      secretName: "ecommerce/jwt/secret"

# ============================================================
# EXEMPLES D'UTILISATION
# ============================================================
#
# Installation avec GHCR (GitHub Container Registry public) - Dev :
# helm install microservices . \
#   --set database.password=XXXXX \
#   --set jwt.secret=YYYYY
#
# Installation sur EKS AWS avec AWS Secrets Manager :
# helm install microservices . \
#   --set awsSecretsManager.enabled=true \
#   --set awsSecretsManager.region=eu-west-1 \
#   --set awsSecretsManager.iamRoleArn=arn:aws:iam::ACCOUNT:role/ecommerce-eks-secrets-role
#
# Images utilisées :
# - ghcr.io/yaraportfolio/auth-service:v3.3
# - ghcr.io/yaraportfolio/product-service:v3.3
# - ghcr.io/yaraportfolio/order-service:v3.3
# - ghcr.io/yaraportfolio/review-service:v3.3
#
# Mise à jour d'un seul service (auth) :
# helm upgrade microservices . \
#   --reuse-values \
#   --set services.authService.image.tag=v3.4
#
# Activer autoscaling :
# helm upgrade microservices . \
#   --reuse-values \
#   --set services.authService.autoscaling.enabled=true
#
# Désactiver un service :
# helm upgrade microservices . \
#   --reuse-values \
#   --set services.reviewService.enabled=false
#
